<nav class="fh5co-nav navbar-fixed-top" role="navigation">
  <div class="top-menu">
    <div class="container">
      <div class="row">
        <div class="col-xs-2">
          <div id="fh5co-logo">
            <%= link_to "CookGrowth", root_path, id:"logo" %>
          </div>
        </div>
        <div class="col-xs-10 text-right menu-1">
          <ul>
            <% if logged_in? %>
            <div class="col-xs-9">
              <li>
                <%= link_to notifications_path do %><span class="fas fa-bell"></span> <span>通知</span><% end %>
              </li>
              <li>
                <%= link_to users_path do %><span class="fas fa-user-friends"></span> <span>ジストモを探す</span><% end %>
              </li>
              <li>
                <%= link_to community_path do %><span class="fa fa-video-camera"></span><span>ジストモ交流会</span><% end %>
              </li>
              <li class="has-dropdown">
                <%= link_to current_user do %>
                  <span class="fas fa-user-cog"></span><span>アカウント</span>
                <% end %>
                <ul class="dropdown">
                  <li><%= link_to 'プロフィール', current_user %></li>
                  <li><%= link_to '設定変更', edit_user_path(current_user) %></li>
                  <li><%= link_to 'ログアウト', logout_path, method: :delete %></li>
                </ul>
              </li>
            </div>
            <div class="col-xs-3">
              <li><%= render "shared/search" %></li>
            </div>
            <% else %>
              <li class="header-link btn-cta">
                <%= link_to login_path do %><span> ログイン</span><% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>